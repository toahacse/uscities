<a href="{{ $href }}" id="{{ $id }}" class="{{ $class }} btn btn-outline bg-{{$color}} btn-icon text-{{$text_color}} btn-sm border-{{$color}} border-2 rounded-round legitRipple mr-1" data-popup="tooltip" title= @lang($tooltip) data-original-title= @lang($tooltip)>
    <i class="fas fa-{{ $icon }}"></i>
    {{ $title }}
</a>
